version: '3.8'

services:
  warpx-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: warpx_app
    ports:
      - "5100:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: always